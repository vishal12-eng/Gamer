services:
  - type: web
    name: futuretechjournal
    env: node
    plan: free
    buildCommand: npm install && npm run build && echo "Build complete"
    startCommand: node server/index.cjs
    envVars:
      - key: NODE_VERSION
        value: 20
