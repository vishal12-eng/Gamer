services:
  - type: web
    name: futuretechjournal
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server/production.cjs
    envVars:
      - key: NODE_VERSION
        value: 20
